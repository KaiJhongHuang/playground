# Playground

這是一個實驗性專案的集合倉庫。

## Git 規範

- 所有程式碼直接推送到 `main` 分支，不使用 feature branch。
- 首頁為 `index.html`，新增專案時請在首頁加入對應的連結卡片。

## 專案結構

- `index.html` — 首頁，列出所有專案的連結與簡介
- 各專案放在獨立的子目錄中（例如 `./my-project/`）
